version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/github_action/
permissions:
  - object: /home/ubuntu/github_action/
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu
